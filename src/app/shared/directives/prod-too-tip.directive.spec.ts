import { ProdTooTipDirective } from './prod-too-tip.directive';

describe('ProdTooTipDirective', () => {
  it('should create an instance', () => {
    const directive = new ProdTooTipDirective();
    expect(directive).toBeTruthy();
  });
});
